<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || "Vanilla Mafia" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Vanilla Mafia">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body class="min-h-screen bg-white font-sans" data-controller="menu">
    <%# Mobile top bar %>
    <header class="lg:hidden flex items-center justify-between bg-neutral-900 px-4 py-3">
      <%= link_to root_path do %>
        <img src="/img/chi_light.png" alt="Vanilla Mafia" class="h-10">
      <% end %>
      <button data-action="menu#toggle" data-menu-target="button"
              class="text-white p-2" aria-label="Menu" aria-expanded="false" aria-controls="mobile-nav">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
    </header>

    <%# Mobile nav (hidden by default) %>
    <nav id="mobile-nav" data-menu-target="nav" class="hidden lg:hidden bg-neutral-900 px-4 pb-4">
      <ul class="space-y-2">
        <% featured_competitions.each do |competition| %>
          <li>
            <%= link_to competition.name, competition_path(slug: competition.slug),
                  class: "block text-gray-300 hover:text-white transition-colors" %>
          </li>
        <% end %>
        <li class="border-t border-neutral-700 pt-2">
          <%= link_to t("nav.hall_of_fame"), hall_path,
                class: "block text-gray-300 hover:text-white transition-colors" %>
        </li>
        <li>
          <%= link_to t("nav.news"), news_index_path,
                class: "block text-gray-300 hover:text-white transition-colors" %>
        </li>
        <% if user_signed_in? %>
          <% if current_user.subscriber? || current_user.admin? %>
            <li>
              <%= link_to t("nav.podcast"), podcast_episodes_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
          <% end %>
          <% if current_user.can_manage_protocols? %>
            <li>
              <%= link_to t("nav.protocols"), judge_protocols_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
          <% end %>
          <% if current_user.can_view_help? %>
            <li>
              <%= link_to t("nav.help"), help_index_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
          <% end %>
          <% if current_user.can_manage_news? %>
            <li>
              <%= link_to t("nav.manage_news"), admin_news_index_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
          <% end %>
          <% if current_user.admin? %>
            <li>
              <%= link_to t("nav.telegram"), admin_telegram_settings_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
            <li>
              <%= link_to t("nav.admin"), avo.root_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
          <% end %>
          <% if current_user.can_manage_news? %>
            <li>
              <%= link_to t("nav.notification_settings"), edit_notification_settings_path,
                    class: "block text-gray-300 hover:text-white transition-colors" %>
            </li>
          <% end %>
          <li class="border-t border-neutral-700 pt-2">
            <%= button_to t("nav.sign_out"), destroy_user_session_path,
                  method: :delete,
                  class: "block text-gray-300 hover:text-white transition-colors" %>
          </li>
        <% else %>
          <li class="border-t border-neutral-700 pt-2">
            <%= link_to t("nav.sign_in"), new_user_session_path,
                  class: "block text-gray-300 hover:text-white transition-colors" %>
          </li>
        <% end %>
        <li class="border-t border-neutral-700 pt-2">
          <%= render "layouts/locale_switcher" %>
        </li>
      </ul>
    </nav>

    <div class="lg:flex lg:min-h-screen">
      <%# Desktop sidebar %>
      <aside class="hidden lg:flex lg:flex-col lg:w-56 lg:flex-shrink-0 bg-neutral-900 bg-repeat bg-top text-gray-300"
             style="background-image: url('/img/back.jpg')">
        <div class="flex flex-col items-center px-4 py-8 bg-neutral-900/80 min-h-screen">
          <%= link_to root_path, class: "mb-8" do %>
            <img src="/img/chi_light.png" alt="Vanilla Mafia" class="w-40">
          <% end %>

          <nav>
            <ul class="space-y-3 text-sm">
              <% featured_competitions.each do |competition| %>
                <li>
                  <%= link_to competition.name, competition_path(slug: competition.slug),
                        class: "block hover:text-white transition-colors" %>
                </li>
              <% end %>
              <li class="border-t border-neutral-700 pt-3">
                <%= link_to t("nav.hall_of_fame"), hall_path,
                      class: "block hover:text-white transition-colors" %>
              </li>
              <li>
                <%= link_to t("nav.news"), news_index_path,
                      class: "block hover:text-white transition-colors" %>
              </li>
              <% if user_signed_in? %>
                <% if current_user.subscriber? || current_user.admin? %>
                  <li>
                    <%= link_to t("nav.podcast"), podcast_episodes_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                <% end %>
                <% if current_user.can_manage_protocols? %>
                  <li>
                    <%= link_to t("nav.protocols"), judge_protocols_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                <% end %>
                <% if current_user.can_view_help? %>
                  <li>
                    <%= link_to t("nav.help"), help_index_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                <% end %>
                <% if current_user.can_manage_news? %>
                  <li>
                    <%= link_to t("nav.manage_news"), admin_news_index_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                <% end %>
                <% if current_user.admin? %>
                  <li>
                    <%= link_to t("nav.telegram"), admin_telegram_settings_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                  <li>
                    <%= link_to t("nav.admin"), avo.root_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                <% end %>
                <% if current_user.can_manage_news? %>
                  <li>
                    <%= link_to t("nav.notification_settings"), edit_notification_settings_path,
                          class: "block hover:text-white transition-colors" %>
                  </li>
                <% end %>
                <li class="border-t border-neutral-700 pt-3">
                  <%= button_to t("nav.sign_out"), destroy_user_session_path,
                        method: :delete,
                        class: "block hover:text-white transition-colors text-left" %>
                </li>
              <% else %>
                <li class="border-t border-neutral-700 pt-3">
                  <%= link_to t("nav.sign_in"), new_user_session_path,
                        class: "block hover:text-white transition-colors" %>
                </li>
              <% end %>
              <li class="border-t border-neutral-700 pt-3">
                <%= render "layouts/locale_switcher" %>
              </li>
            </ul>
          </nav>
        </div>
      </aside>

      <%# Main content %>
      <main class="flex-1 px-4 py-8 lg:px-12 lg:py-10 max-w-5xl">
        <%= render "devise/shared/flash" %>
        <%= yield %>
      </main>
    </div>

    <% if @announcements&.any? && FeatureToggle.enabled?("toast_whats_new") %>
      <%= render "layouts/announcement_toast", announcements: @announcements %>
    <% end %>
  </body>
</html>
